#!/usr/bin/env bash
# ReposMx Server launcher
PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec julia --project="$PROJECT_DIR" -e 'using ReposMx; ReposMx.main(["serve", ARGS...])' -- "$@"
