

    #=open(outname, "w") do out
        for d in D
            println(out, json(d))
        end
    end=#
#=for filename in glob("repositorios/metadata-2024-05-29/*.xml")
    parse_xml_repo(filename)
end =#
